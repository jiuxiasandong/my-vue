
// 进一步 处理 state   数据 
export default {
    mv: state => {
        // const { descData } = state;
        // try {
        //     descData.mv.genres = descData.mv.genres.join("/");
        //     descData.mv.casts = descData.mv.casts.map(item => item.name).join("/");
        //     descData.mv.rate = descData.mv.rating.average;
        //     return descData.mv;
        // } catch (error) { }
    }

}